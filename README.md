This Java program collects user information and validates each input using exception handling. The system ensures that all user-provided data meets specific requirements before being accepted.

Invalid inputs are handled using a custom exception class, and users are re prompted until valid data is entered.
Hemish R Mandadi
